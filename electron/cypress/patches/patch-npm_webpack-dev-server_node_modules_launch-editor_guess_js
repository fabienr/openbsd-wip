Index: npm/webpack-dev-server/node_modules/launch-editor/guess.js
--- npm/webpack-dev-server/node_modules/launch-editor/guess.js.orig
+++ npm/webpack-dev-server/node_modules/launch-editor/guess.js
@@ -73,7 +73,7 @@ module.exports = function guessEditor (specifiedEditor
           return [fullProcessPath]
         }
       }
-    } else if (process.platform === 'linux') {
+    } else if (process.platform === 'linux' || process.platform === 'openbsd') {
       // --no-heading No header line
       // x List all processes owned by you
       // -o comm Need only names column
