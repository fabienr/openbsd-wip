Index: node_modules/electron-packager/src/targets.js
--- node_modules/electron-packager/src/targets.js.orig
+++ node_modules/electron-packager/src/targets.js
@@ -5,9 +5,10 @@ const { getHostArch } = require('@electron/get')
 const semver = require('semver')
 
 const officialArchs = ['ia32', 'x64', 'armv7l', 'arm64', 'mips64el', 'universal']
-const officialPlatforms = ['darwin', 'linux', 'mas', 'win32']
+const officialPlatforms = ['darwin', 'linux', 'mas', 'win32', 'openbsd']
 const officialPlatformArchCombos = {
   darwin: ['x64', 'arm64', 'universal'],
+  openbsd: ['x64', 'arm64', 'universal'],
   linux: ['ia32', 'x64', 'armv7l', 'arm64', 'mips64el'],
   mas: ['x64', 'arm64', 'universal'],
   win32: ['ia32', 'x64', 'arm64']
@@ -18,6 +19,10 @@ const buildVersions = {
     arm64: '>= 11.0.0-beta.1',
     universal: '>= 11.0.0-beta.1'
   },
+  openbsd: {
+    arm64: '>= 11.0.0-beta.1',
+    universal: '>= 11.0.0-beta.1'
+  },
   linux: {
     arm64: '>= 1.8.0',
     ia32: '<19.0.0-beta.1',
@@ -36,6 +41,7 @@ const buildVersions = {
 const osModules = {
   darwin: './mac',
   linux: './linux',
+  openbsd: './linux',
   mas: './mac', // map to darwin
   win32: './win32'
 }
