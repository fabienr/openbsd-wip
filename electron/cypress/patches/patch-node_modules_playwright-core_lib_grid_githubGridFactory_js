Index: node_modules/playwright-core/lib/grid/githubGridFactory.js
--- node_modules/playwright-core/lib/grid/githubGridFactory.js.orig
+++ node_modules/playwright-core/lib/grid/githubGridFactory.js
@@ -46,7 +46,7 @@ const githubFactory = {
 };
 
 async function createWorkflow(inputs) {
-  if (!['windows', 'linux', 'macos'].includes(inputs.os)) {
+  if (!['windows', 'linux', 'macos', 'openbsd'].includes(inputs.os)) {
     log(`unsupported OS: ${inputs.os}`);
     return false;
   }
