Index: node_modules/app-builder-lib/out/core.js
--- node_modules/app-builder-lib/out/core.js.orig
+++ node_modules/app-builder-lib/out/core.js
@@ -36,6 +36,8 @@ class Platform {
                 return Platform.WINDOWS;
             case Platform.LINUX.nodeName:
                 return Platform.LINUX;
+            case Platform.OPENBSD.nodeName:
+                return Platform.OPENBSD;
             default:
                 throw new Error(`Unknown platform: ${name}`);
         }
@@ -44,6 +46,7 @@ class Platform {
 exports.Platform = Platform;
 Platform.MAC = new Platform("mac", "mac", "darwin");
 Platform.LINUX = new Platform("linux", "linux", "linux");
+Platform.OPENBSD = new Platform("openbsd", "openbsd", "openbsd");
 Platform.WINDOWS = new Platform("windows", "win", "win32");
 class Target {
     constructor(name, isAsyncSupported = true) {
